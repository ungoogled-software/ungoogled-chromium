#!/bin/bash

set -eux

FILENAME=ungoogled-chromium_$ungoog{version_string}_linux.tar.xz
OUTPUT=$(dirname $(dirname $(readlink -f $0)))/$FILENAME

# TODO: Use scripts/list_build_outputs.py to create a .tar.xz archive of build outputs using tar
